cohort_description
- Type string
	- no test possible

cohort_dose
- Type number
	- input string "notanumber" in row 1

NOTE: Our ready-for-loading text file (NCATS01-cohort.txt) contains an extra column labeled cohort_id.  This is NOT a property included in the yaml database files.  It is NOT being tested for now.